info:
  name: Negative - Invalid UUID
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/api/v1/services/123"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
