info:
  name: Get Service by Code
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/v1/services/code/modules-version2.2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
