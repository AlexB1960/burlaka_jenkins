info:
  name: Get All Services
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/services"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
