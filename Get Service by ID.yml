info:
  name: Get Service by ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/services/{{serviceId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
