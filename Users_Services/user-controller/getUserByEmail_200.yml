info:
  name: getUserByEmail_200
  type: http
  seq: 7
  tags:
    - user-controller

http:
  method: GET
  url: "{{baseUrl}}/api/user/email/:email"
  params:
    - name: email
      value: string@dsf.ru
      type: path
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status cod = 200", function () {
            expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/user/email/:email"
      method: GET
      params:
        - name: email
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "message": "",
            "status": "OK",
            "payload": {
              "user": {
                "id": "",
                "login": "",
                "email": "",
                "fio": {
                  "firstName": "",
                  "lastName": "",
                  "secondName": ""
                }
              }
            }
          }
