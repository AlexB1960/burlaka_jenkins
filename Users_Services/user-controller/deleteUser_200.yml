info:
  name: deleteUser_200
  type: http
  seq: 3
  tags:
    - user-controller

http:
  method: DELETE
  url: "{{baseUrl}}/api/user/:id"
  params:
    - name: id
      value: "{{userId}}"
      type: path
  auth: inherit

runtime:
  variables:
    - name: userId
      value: ""
  scripts:
    - type: tests
      code: |-
        test("Status cod = 200", function () {
            expect(res.getStatus()).to.equal(200);
        });

        test("Status = OK", function () {
            expect(res.res.statusText).to.equal("OK");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/user/:id"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "message": "",
            "status": "OK",
            "payload": {
              "user": {
                "id": "",
                "login": "",
                "email": "",
                "fio": {
                  "firstName": "",
                  "lastName": "",
                  "secondName": ""
                }
              }
            }
          }
